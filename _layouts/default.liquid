<!doctype html>
<html lang="pl">
  {% include head.liquid %}

  <body>
    {% include header/index.liquid %}

    <main>
      {% unless page.layout == 'home' %}
        {% include hero.liquid %}
      {% endunless %}

      {{ content }}
    </main>

    {% include footer/index.liquid %}
  </body>
</html>
